- `tauri.conf.json` 已限制 `minWidth: 1100` / `minHeight: 720`，无需考虑小屏适配
- UI 允许超前，后端如果还未实现，交互时只提示/无反应均可，但不要删除或隐藏相关 UI 元素
- 由于产品还未上线，数据库迁移请尽量以最小成本实现，数据丢失也可以接受
- 遇到问题时优先参考 `${ROOT}/Sample/cc-switch` 中的成熟实现
